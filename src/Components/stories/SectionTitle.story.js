import React from 'react'
import { storiesOf } from '@storybook/react'
import SectionTitle from '../SectionTitle'

storiesOf('Section Title', module).add('default', () => (
  <SectionTitle title='Section Title' />
))
